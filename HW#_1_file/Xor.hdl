
/**
 * Exclusive-or gate:
 * out = (((a == 0) & (b = 1)) | ((a == 1) & (b = 0)), 1, 0)
 */
CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
   Nand  (a=a,b=a,out=w1); 
Nand  (a=b,b=b, out=w2);
Nand  (a=w1,b=b,out=w3); 
Nand  (a=w2,b=a, out=w4);
Nand (a=w3,b= w4,out=out); 
}